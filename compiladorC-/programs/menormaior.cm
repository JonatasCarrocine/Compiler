/* Um programa para verificar se os valores digitados pelo usuarios sao iguais, menor ou maior
Retorna 1 se 'a' for menor que 'b'
Retorna 2 se 'b' for menor que 'a'
Retorna 0 se valores forem iguais*/

int menormaior (int a, int b)
{
    if (a < b) return 1;
    else if (a > b) return 2;
    else return 0;
}

void main(void)
{
    int avalor;
    int bvalor;
    
    avalor = input();
    bvalor = input();

    output(menormaior(avalor, bvalor));
}