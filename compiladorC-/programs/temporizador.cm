/* Um programa para realizar uma contagem regressiva para 0*/

int tempo(int a) {
    if (a == 0) return 0;
    else return tempo(a - 1);
}

void main(void) {
    int x;
    int valor;

    x = input();

    valor = tempo(x);
}